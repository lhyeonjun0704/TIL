html 빠르게 작성할 수 있게 해주는 것.
 - 자주 쓰는 애들은 빨리 할 수 있게 해야되는데....
 - Emmet플러그인.
   -> 요즘은 다 들어가 있다.
   -> !html 형식
   -> div + tab 만들어짐
   -> #~~~ id 자동 생성
   -> 부모, 자식, 형제 노드? div>ul>li + tab은 자동 생성
   -> ul>li*5 이런식으로 자동으로 5개 생성.
   -> div>ul>li^ol (^는 형제 노드)
   -> div>(header>ul>li*2>a)+footer>p(+ 형제노드)
   -> p{hello} + tab 텍스트 생성가능
   -> $는 숫자 할당 가능 (ex)p.class${item $}*5 이런식으로 가능
   -> p>lorem도 가능(뒤에 숫자 넣으면 넣고싶은 단어 개수 조정 가능)
   -> css에서 클래스 자동완성은 vsc에서 다운 가능.
   -> UI Compo -> Web(실제 사이트 사용)