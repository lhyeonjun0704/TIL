/* ||(or), &&(and), !(not) * check()=true/
/* nullobject.something? */

/* == loose equality, with type == / != */
/* strict equality, no type conversion === / !== 타입이 다를 때 구분 가능 */

/* Ternary operator : ? */
console.log(name === 'ellie' ? 'yes': 'no'); /* ?왼쪽 조건이 true일 때 왼쪽 yes실행, false이면 오른쪽 no 실행  중첩하면 가독성 떨어진다*/

/* while */
let i = 3;
while(i > 0){
    console.log('while: ${i}');
    i--;
}
/* do while 블럭 선실행 후 조건 검사 */

/* for 루프는 c하고 같네 */

